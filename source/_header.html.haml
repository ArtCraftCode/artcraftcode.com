.header
  %img{ src: pattern.images[0] }
  %h1.over-image
    = pattern.name
  .section.no-bottom
    %h2= pattern.description
  = partial(:images_2, locals: { images: forest_park.images })
.sidebar-right